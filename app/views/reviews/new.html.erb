<h1>New Review</h1>
<%= simple_form_for [@restaurant, @review] do |form| %>
  <%= form.input :content %>
  <%= form.input :rating, as: :select, collection: (0..5) %>
  <%= form.submit 'Save review', class: 'btn btn-primary' %>
<% end %>
